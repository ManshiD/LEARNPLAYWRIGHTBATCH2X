let fruits = []; // This is an array literal assigned to the variable 'fruits' containing three string elements
let fruits_fresh = ['apple', 'banana', 'cherry']; // This is an array literal assigned to the variable 'fruits_fresh' containing three string elements

let arr = [10, 20, 30, 40];
console.log(arr[0]); // Output: 10
console.log(arr[3]); // Output: 40
console.log(arr[4]); // Output: undefined (index out of bounds)

let testResults = ["Pass", "Fail", "Pass", "Skip"]; // This is an array literal assigned to the variable 'testResults' containing four string elements
let mixed = [1, "hello", true, null]; // This is an array literal assigned to the variable 'mixed' containing four elements of different types
